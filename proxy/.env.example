# Anthropic API key for the managed tier. Server-side only — never exposed to the extension.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: pin a workspace, set a base URL, etc. See the Anthropic SDK docs.
# ANTHROPIC_BASE_URL=
